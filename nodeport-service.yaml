apiVersion: v1
kind: Pod
metadata:
  name: docker-cv
  labels:
    name: docker-cv
spec:
  containers:
    - name: docker-cv
      image: taufik1/docker-cv:v1

---

apiVersion: v1
kind: Service
metadata:
  name: cv-service
spec:
  type: NodePort
  selector:
    name: docker-cv
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001